# SDMon V2 Normalized Event schema.
# This file is declarative and is consumed by trusted SDMon scripts.

EVENT_SCHEMA_VERSION="1"

EVENT_FIELDS="EVENT_ID EVENT_VERSION TIMESTAMP HOSTNAME USER_NAME PLATFORM SENSOR_TYPE PROFILE_ID EVENT_CATEGORY EVENT_TYPE PROCESS_NAME PID ACTION TARGET TARGET_TYPE SOURCE CONFIDENCE TAGS SERVICE_TYPE PLIST_PATH LABEL PROTOCOL LOCAL_ADDRESS LOCAL_PORT REMOTE_ADDRESS REMOTE_PORT COMPUTER_NAME OS_NAME OS_VERSION OS_BUILD KERNEL ARCH PERMISSION_TYPE STATUS DETAIL"

EVENT_REQUIRED_FIELDS="EVENT_ID EVENT_VERSION TIMESTAMP PLATFORM SENSOR_TYPE PROFILE_ID EVENT_CATEGORY EVENT_TYPE ACTION TARGET TARGET_TYPE SOURCE CONFIDENCE"
